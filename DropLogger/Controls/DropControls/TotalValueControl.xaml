<UserControl x:Class="DropLogger.TotalValueControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:DropLogger"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{x:Static core:LootValue.Instance}">
    
    <!-- Complete value -->
    <TextBlock Text="{Binding formattedValue, FallbackValue=0000}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="25"
               FontWeight="Bold"
               Grid.Row="0"
               Grid.Column="3"
               Foreground="{StaticResource RichBlackFOGRAColorBrush}"
               />
    
</UserControl>
